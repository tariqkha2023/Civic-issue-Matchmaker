services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: civic_matchmaker
      POSTGRES_USER: civic_user
      POSTGRES_PASSWORD: civic_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: